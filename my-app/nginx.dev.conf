events { }

http {
  server {
    listen 80;

    location / {
      proxy_pass http://frontend:5173;
    }

    # Teruskan /api/ ke root backend
    location /api/ {
      proxy_pass http://backend:3001/;
    }
  }
}

